# 면접 스터디 피드백 앱 — 기술 명세서

## 프로젝트 개요

면접 준비 스터디원들이 모의면접 영상을 공유하고, **타임스탬프 기반 피드백**을 주고받는 iOS 앱.

---

## 1. 기술 스택

### 1-1. iOS 클라이언트

| 영역 | 스택 | 선택 이유 |
|------|------|-----------|
| UI | **SwiftUI** | 선언적 UI, 빠른 프로토타이핑 |
| 아키텍처 | **TCA (The Composable Architecture)** | Feature 단위 모듈화, 테스트 용이 |
| 영상 재생 | **AVKit / AVPlayer** | 타임스탬프 제어, 커스텀 컨트롤 필수 |
| 영상 선택 | **PhotosUI (PhotosPicker)** | iOS 16+ 네이티브, 권한 관리 간소화 |
| 네트워크 | **Supabase Swift SDK** | Auth/DB/Storage 통합 클라이언트 |
| 푸시 알림 | **FCM + APNs** | 새 피드백 알림 |
| 이미지 캐싱 | **Kingfisher** | 프로필/썸네일 캐싱 |

### 1-2. 백엔드 (Supabase BaaS)

| 영역 | 스택 | 선택 이유 |
|------|------|-----------|
| 인증 | **Supabase Auth** | 카카오/Apple 소셜 로그인 지원 |
| DB | **PostgreSQL (Supabase)** | 관계형 쿼리 (피드백 필터링, 스터디별 조회) |
| 접근 제어 | **Row Level Security** | 스터디 멤버만 영상/피드백 접근 가능 |
| 영상 저장 | **Supabase Storage** | 4분 미만 영상, 버킷 단위 관리 |
| 실시간 | **Supabase Realtime** | 새 피드백/영상 업로드 실시간 반영 |
| 서버 로직 | **Supabase Edge Functions** | 초대코드 검증, 푸시 발송 등 |

### 1-3. 인프라

| 영역 | 스택 |
|------|------|
| CI/CD | Xcode Cloud 또는 Fastlane + GitHub Actions |
| 크래시 리포트 | Firebase Crashlytics |
| 분석 | Firebase Analytics (경량, 무료) |
| 영상 썸네일 | AVAssetImageGenerator (클라이언트 생성) |
| 딥링크 | Universal Link (초대코드 공유 → 앱 직접 진입) |

---

## 2. 화면 구조 및 기능 명세

앱은 **3개 탭** 구조로 구성된다.

### Tab 1 — 스터디 (메인)

#### 2-1. 스터디 목록 화면

- 내가 참여 중인 스터디를 **카드 리스트**로 표시
- 카드 정보: 스터디명, 참여인원, 최근 활동일, 새 피드백 뱃지
- 상단 액션: **초대코드 입력 버튼** + **스터디 생성 버튼**

#### 2-2. 스터디 생성 플로우

1. 스터디명 입력 (예: "25년 상반기 iOS 면접 스터디")
2. 최대 인원 설정 (추천: 4~8명)
3. 생성 완료 → 초대코드 자동 발급 → 공유 시트 표시

#### 2-3. 스터디 상세 (입장 후)

- **멤버 목록**: 프로필 사진 + 닉네임
- **영상 피드** (최신순): 업로더명, 업로드일, 피드백 수 표시
- 우측 하단 **FAB** → 영상 업로드 진입

#### 2-4. 영상 업로드 플로우

1. 갤러리에서 영상 선택 (4분 미만 제한, 초과 시 토스트 안내)
2. 제목 입력 (예: "카카오 1차 모의면접")
3. 업로드 중 프로그레스 표시 → 완료 후 피드에 노출

#### 2-5. 영상 상세 화면

- **상단 고정**: 영상 플레이어
- **하단**: 피드백 리스트 (타임스탬프 + 작성자 + 내용)
- **피드백 작성 시**:
  - 현재 재생 시점 자동 캡처
  - 대상자 태깅(`@`) + 텍스트 입력
- **피드백 탭 시**: 해당 타임스탬프로 영상 자동 이동 (seek)

---

### Tab 2 — 피드백 관리

상단 **세그먼트 컨트롤** 2개: `받은 피드백` | `해준 피드백`

#### 2-6. 받은 피드백

- 내 영상에 달린 피드백을 최신순 나열
- 셀 정보: 스터디명, 영상 제목, 피드백 작성자, 내용 미리보기, 날짜
- 탭 → 해당 영상 상세로 이동 (타임스탬프 위치로 seek)

#### 2-7. 해준 피드백

- 내가 다른 사람 영상에 남긴 피드백
- 동일한 셀 구조 + 대상자명 추가 표시

---

### Tab 3 — 설정

- 프로필 편집 (닉네임, 프로필 이미지)
- 알림 설정 (새 피드백 알림 ON/OFF)
- 참여 중인 스터디 관리 (나가기)
- 이용약관 / 개인정보처리방침
- 앱 버전
- 로그아웃 / 회원탈퇴

---

## 3. 핵심 비즈니스 규칙

| 규칙 | 상세 |
|------|------|
| 영상 길이 제한 | 4분 미만만 업로드 가능 |
| 접근 권한 | 스터디 멤버만 해당 스터디의 영상/피드백 열람 가능 (RLS) |
| 초대코드 | 스터디 생성 시 자동 발급, Universal Link로 공유 |
| 피드백 타임스탬프 | 작성 시점의 영상 재생 위치(초 단위)를 자동 저장 |
| 실시간 반영 | 새 피드백/영상 업로드 시 Supabase Realtime으로 즉시 반영 |
| 소셜 로그인 | 카카오, Apple 로그인 지원 |

---

## 4. 최소 지원 환경

- **iOS 16.0+**
- **Swift 5.9+**
- **Xcode 15+**
